\usepackage[ruled,lined,linesnumbered,inoutnumbered,longend]{algorithm2e}
\SetAlgoCaptionSeparator{:~}
\SetAlgorithmName{Algoritmo}{Algoritmo}{Listado de algoritmos}
\SetAlFnt{\scriptsize}
\SetAlCapFnt{\scriptsize}
\SetAlCapNameFnt{\scriptsize}
\setlength{\algomargin}{2em}
\SetKwInput{Input}{input}
\SetKwInput{Output}{output}
\SetAlgoCaptionLayout{small}
\SetKwBlock{BeginSubMain}{begin sub main}{end sub}
\SetKwBlock{BeginSubSumarDias}{begin sub sumarDias}{end sub}
% to avoid italics in algorithms
%\SetArgSty{tt}
%\SetAlFnt{\sf\ttfamily}

%para los algoritmos
\newenvironment{newAlgorithm2e}
{ \begin{algorithm}[H] \DontPrintSemicolon \BlankLine}
{ \BlankLine\BlankLine  \end{algorithm}}

\usepackage{listingsutf8}
 
\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}
\definecolor{bluebradley}{RGB}{46,106,177}
 
\lstset{ %
  language={java},                % the language of the code
  basicstyle=\lst@ifdisplaystyle\footnotesize\fi\ttfamily,  % the size of the fonts that are used for the code
  %basicstyle=\scriptsize\ttfamily,  % the size of the fonts that are used for the code
  numbers=none,                   % where to put the line-numbers
  %numbers=left,                   % where to put the line-numbers
  %numberstyle=\tiny\color{gray},  % the style that is used for the line-numbers
  stepnumber=1,                   % the step between two line-numbers. If it's 1, each line 
                                  % will be numbered
  numbersep=9pt,                  % how far the line-numbers are from the code
  backgroundcolor=\color{white},      % choose the background color. You must add \usepackage{color}
  showspaces=false,               % show spaces adding particular underscores
  showstringspaces=false,         % underline spaces within strings
  showtabs=false,                 % show tabs within strings adding particular underscores
  frame=single,                   % adds a frame around the code
  rulecolor=\color{black},        % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
  tabsize=4,                      % sets default tabsize to 2 spaces
  captionpos=b,                   % sets the caption-position to bottom
  breaklines=true,                % sets automatic line breaking
  breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
%  title=\lstname,                   % show the filename of files included with \lstinputlisting;
                                  % also try caption instead of title
  keywordstyle=\color{blue},          % keyword style
  keywordstyle=[5]\color[RGB]{43,145,175},          % keyword style
  commentstyle=\color{dkgreen},       % comment style
  stringstyle=\color{mauve},         % string literal style
  escapeinside={\%*}{*)},            % if you want to add a comment within your code
  extendedchars=true,
  literate={ú}{{\'u}}1 {á}{{\'a}}1 {é}{{\'e}}1 {í}{{\'i}}1 {ó}{{\'o}}1 {ñ}{{\~n}}1 {Ú}{{\'u}}1 ,
%  inputencoding=utf8/latin1,
  morekeywords={As,Each,In,New,Double,Contains,Module,Loop,Until,To,Continue,Or,And,Boolean,ByVal,ByRef,Step,Imports,True,False,*,...},               % if you want to add more keywords to the set
  deletekeywords=[4]{Write},
  morekeywords=[5]{Console,StreamReader,StreamWriter,List,Encoding,Thread,CultureInfo,Environment,SpecialFolder,Math},
  columns=flexible
}
%\lstset{aboveskip=9pt plus 2pt, belowskip=-9pt plus 2pt}
%\lstset{aboveskip=9pt plus 2pt, belowskip=-2pt plus 2pt}

\lstnewenvironment{syntaxdef}[1][]%
  {\lstset{backgroundcolor=,basicstyle=\ttfamily,frame=,numbers=none,aboveskip=-4pt,belowskip=-1mm,
      moredelim=*[is][\textit]{|}{|},
      %moredelim=*[is][\color{blue}\textbf]{|-}{-|},deletekeywords=[1]{#1},deletekeywords=[2]{#1}
      moredelim=[is][\color{blue}\textbf]{|-}{-|},deletekeywords=[1]{#1},deletekeywords=[2]{#1}
  }}{}

\renewcommand{\lstlistingname}{Listado}
